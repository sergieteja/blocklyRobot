'use strict';

goog.provide('Blockly.Python.Perceptual');

goog.require('Blockly.Python');


Blockly.Python['back_obstacle'] = function(block) {

  var code = 'back_obstacle()\n'

  return code;
};

Blockly.Python['center_black_line'] = function(block) {
  // TODO: Assemble JavaScript into code variable.
  var code = 'center_black_line\n';
  return code;
};

Blockly.Python['move_right'] = function(block) {
  // TODO: Assemble JavaScript into code variable.
  var code = 'move_right()\n';
  return code;
};

Blockly.Python['center_red_line'] = function(block) {
  // TODO: Assemble JavaScript into code variable.
  var code = 'center_red_line()\n';
  return code;
};

Blockly.Python['front_obstacle'] = function(block) {
  // TODO: Assemble JavaScript into code variable.
  var code = 'front_obstacle()\n';
  return code;
};

Blockly.Python['get_distance'] = function(block) {
  // TODO: Assemble JavaScript into code variable.
  var code = 'get_distance()\n';
  return code;
};

Blockly.Python['get_image'] = function(block) {
  // TODO: Assemble JavaScript into code variable.
  var code = 'get_image()\n';
  return code;
};

Blockly.Python['left_black_line'] = function(block) {
  // TODO: Assemble JavaScript into code variable.
  var code = 'left_black_line()\n';
  return code;
};

Blockly.Python['left_obstacle'] = function(block) {
  // TODO: Assemble JavaScript into code variable.
  var code = 'left_obstacle()\n';
  return code;
};

Blockly.Python['left_red_line'] = function(block) {
  // TODO: Assemble JavaScript into code variable.
  var code = 'left_red_line()\n';
  return code;
};


Blockly.Python['line_crossing'] = function(block) {
  // TODO: Assemble JavaScript into code variable.
  var code = 'line_crossing()\n';
  return code;
};

Blockly.Python['obstacle_free'] = function(block) {
  // TODO: Assemble JavaScript into code variable.
  var code = 'obstacle_free()\n';
  return code;
};


Blockly.Python['right_black_line'] = function(block) {
  // TODO: Assemble JavaScript into code variable.
  var code = 'right_black_line()\n';
  return code;
};

Blockly.Python['right_obstacle'] = function(block) {
  // TODO: Assemble JavaScript into code variable.
  var code = 'right_obstacle()\n';
  return code;
};

Blockly.Python['right_red_line'] = function(block) {
  // TODO: Assemble JavaScript into code variable.
  var code = 'right_red_line()\n';
  return code;
};

Blockly.Python['visual_auxiliary'] = function(block) {
  // TODO: Assemble JavaScript into code variable.
  var code = 'visual_auxiliary()\n';
  return code;
};