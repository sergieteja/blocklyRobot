'use strict';

goog.provide('Blockly.Python.Accion');

goog.require('Blockly.Python');


Blockly.Python['camino_libre'] = function(block) {

  var code = 'camino_libre()\n'

  return code;
};

Blockly.Python['avanzar'] = function(block) {
  // TODO: Assemble JavaScript into code variable.
  var code = 'avanzar()\n';
  return code;
};

Blockly.Python['gira_izq'] = function(block) {
  // TODO: Assemble JavaScript into code variable.
  var code = 'girar_izq()\n';
  return code;
};

Blockly.Python['sigue_linea'] = function(block) {
  // TODO: Assemble JavaScript into code variable.
  var code = 'sigue_linea()\n';
  return code;
};

Blockly.Python['parar_base'] = function(block) {
  // TODO: Assemble JavaScript into code variable.
  var code = 'parar_base()\n';
  return code;
};